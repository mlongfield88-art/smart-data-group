---
const currentYear = new Date().getFullYear();

const footerLinks = {
  company: [
    { href: '/about', label: 'About Us' },
    { href: '/news', label: 'News' },
    { href: '/events', label: 'Events' },
    { href: '/contact', label: 'Contact' },
  ],
  divisions: [
    { href: '/about', label: 'SDG UK' },
    { href: '/about', label: 'Smart Data Services' },
    { href: '/about', label: 'Smart Data International' },
    { href: '/contact', label: 'Membership' },
  ],
  connect: [
    { href: 'https://www.linkedin.com/company/smart-data-group-uk', label: 'LinkedIn' },
    { href: 'https://x.com/smartdatagroup', label: 'X / Twitter' },
  ],
};
---

<footer class="footer section">
  <div class="container">
    <div class="footer__top">
      <div class="footer__brand">
        <div class="footer__logo">
          <img src="/images/logo-transparent.png" alt="Smart Data Group" class="footer__logo-img" width="180" height="44" />
        </div>
        <p class="footer__tagline text-body-lg">
          Empowering members to design and implement smart data schemes — unlocking innovation, better services, and economic growth across the UK economy.
        </p>
      </div>

      <div class="footer__links-grid">
        <div class="footer__col">
          <h4 class="text-label">Company</h4>
          <ul>
            {footerLinks.company.map((link) => (
              <li><a href={link.href} class="footer__link">{link.label}</a></li>
            ))}
          </ul>
        </div>
        <div class="footer__col">
          <h4 class="text-label">Divisions</h4>
          <ul>
            {footerLinks.divisions.map((link) => (
              <li><a href={link.href} class="footer__link">{link.label}</a></li>
            ))}
          </ul>
        </div>
        <div class="footer__col">
          <h4 class="text-label">Connect</h4>
          <ul>
            {footerLinks.connect.map((link) => (
              <li><a href={link.href} class="footer__link" target="_blank" rel="noopener">{link.label}</a></li>
            ))}
          </ul>
        </div>
      </div>
    </div>

    <div class="divider" style="margin: var(--space-10) 0 var(--space-6);"></div>

    <div class="footer__bottom">
      <p class="text-caption">&copy; {currentYear} Smart Data Group. All rights reserved.</p>
      <div class="footer__legal">
        <a href="#" class="text-caption footer__legal-link">Privacy Policy</a>
        <a href="#" class="text-caption footer__legal-link">Terms of Use</a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-bg);
    border-top: 1px solid var(--color-border);
  }

  .footer__top {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--space-16);
  }

  .footer__logo {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-6);
  }

  .footer__logo-img {
    height: 40px;
    width: auto;
    object-fit: contain;
  }

  .footer__tagline {
    max-width: 360px;
  }

  .footer__links-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
  }

  .footer__col h4 {
    margin-bottom: var(--space-5);
  }

  .footer__col ul {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .footer__link {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    transition: color var(--duration-fast) ease;
  }

  .footer__link:hover {
    color: var(--color-text-primary);
  }

  .footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .footer__legal {
    display: flex;
    gap: var(--space-6);
  }

  .footer__legal-link {
    transition: color var(--duration-fast) ease;
  }

  .footer__legal-link:hover {
    color: var(--color-text-primary);
  }

  @media (max-width: 768px) {
    .footer__top {
      grid-template-columns: 1fr;
      gap: var(--space-10);
    }

    .footer__links-grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
    }

    .footer__bottom {
      flex-direction: column;
      gap: var(--space-4);
      text-align: center;
    }
  }
</style>
